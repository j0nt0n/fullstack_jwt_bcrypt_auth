networks:
  dev:

services:
  nginx1:
    image: nginx:stable-alpine
    ports:
      - "81:81"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend:
    build:
      context: ./server
    networks:
      - dev

  frontend:
    build:
      context: ./client
    networks:
      - dev
